#include "../includes/conway.hpp"

int main(void)
{
	if (SDL_Init(SDL_INIT_EVERYTHING) != 0)
		std::cout << "error initializing SDL" << std::endl;
	// SDL_Window *win = SDL_CreateWindow("Test", 0, 0, 1000, 1000, 0);
	// while(1)
	// 	;
	// return (0);
}